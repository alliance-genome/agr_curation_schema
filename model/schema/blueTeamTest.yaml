id: https://github.com/alliance-genome/agr_curation_schema/src/schema/blueTeamTest.yaml
name: Blue-Team-Test
title: Blue Team Test

imports:
  - core
  - allianceMember

prefixes:
  alliance: "http://alliancegenome.org/"
  linkml: "https://w3id.org/linkml/"
  gff: "https://w3id.org/gff"
  faldo: "http://biohackathon.org/resource/faldo#"
  biolink: "https://w3id.org/biolink/vocab/"
  NLMID: "https://www.ncbi.nlm.nih.gov/nlmcatalog/?term="
  schema: "http://schema.org/"
  ECO: "http://purl.obolibrary.org/obo/ECO_"

default_prefix: alliance
default_range: string

default_curi_maps:
  - obo_context
  - idot_context
  - semweb_context
  - monarch_context

emit_prefixes:
  - rdf
  - rdfs
  - xsd
  - owl


## ------------
## CLASSES
## ------------


classes:

  TopicEntityTagSource:
    is_a: AuditedObject
    description: >-
      Topic entity tag source (tets)
    slots:
      - topic_entity_tag_source_id
      - source_type
      - source_method
      - validation_type
      - tets_description
      - evidence_code
      - alliance_member
    slot_usage:
      source_type:
        required: true
      source_method:
        required: true
      validation_type:
        required: false
      tets_description:
        required: false
      evidence_code:
        required: true
      alliance_member:
        required: true

  TopicEntityTag:
    is_a: AuditedObject
    description: >-
      Topic entity tag
    slots:
      - topic_entity_tag_source
      - single_reference
      - atp_topic
      - tagged_entity
      - atp_entity_type
      - tagged_entity_source
      - entity_published_as
      - taxon
      - display_tag
      - negated
      - tet_confidence_level
      - related_note
    slot_usage:
      single_reference:
        required: true
      atp_topic:
        required: true




## ------------
## SLOTS
## ------------

# Guidelines:
# Please provide a range for all slots

slots:

  alliance_member:
    range: AllianceMember

  atp_entity_type:
    description: >-
      An ATP term that specifies an entity type (subset of the ATP ontology - branch "entity type" ATP:0000122)
      domain: TopicEntityTag
      range: ATPTerm

  atp_topic:
    description: >-
      An ATP term that specifies a topic (subset of the ATP ontology - branch "topic tag" ATP:0000002)
    domain: TopicEntityTag
    range: ATPTerm

  display_tag:
    description: >-
      SGD specific field - maps to "topic" in SGD
    domain: TopicEntityTag
    range: string

  entity_published_as:
    description: >-
      This is the name or synonym of the tagged_entity mentioned in the single_reference
    domain: TopicEntityTag
    range: string

  source_method:
    range: string
  
  source_type:
    range: string
    notes: Free text for the source type

  tagged_entity:
    range: BiologicalEntity # this may not include taxon IDs, but it depends on the implementation
    domain: TopicEntityTag

  tagged_entity_source:
    range: Organization
    domain: TopicEntityTag
    description: >-
      This is used to identify the API where to validate the tagged_entity against
    notes: >-
      Revisit in the future to make sure we can cover Alliance and MODs as sources

  tet_confidence_level:
    description: >-
      Confidence level of the prediction. Used only for tags generated by AI/ML.
    domain: TopicEntityTag
    range: VocabularyTerm

  tets_description:
    description: >-
      The topic entity tag source description
    domain: TopicEntityTagSource
    range: string

  topic_entity_tag_source:
    description: >-
      The topic entity tag source that generated a specific topic entity tag
    domain: TopicEntityTag
    range: TopicEntityTagSource
    required: true
    multivalued: false

  topic_entity_tag_source_id:
    description: >-
      Unique ID for the source
    notes: >-
      automatically incremented by the software
    domain: TopicEntityTagSource
    range: string
    required: true
    identifier: true

  validation_type:
    range: VocabularyTerm 
    notes: CV includes "null", "curator", "author", "curation_tool"

  



